//use strict
"use strict";//treat all JS code as newer version
//alert(3+3); //This will give error in nodejs but when we use same code in browser console it will work becaz browser has inbuilt JS engine

//Primitive Data types
let name="Hemant"//string " "/' '
let age=20;//Number 
let isLoggin=true;//boolean -> true/false

//bigint->if range is greater than Number than we use this type
let temperature=null;//null -> It is data type as well as value ->empty
let email;//undefined -> when we don't define value
//symbol->
//-------------------------------------------------------------------------//

//Object

console.log(typeof name);
console.log(typeof null);// object
console.log(typeof email); //undefined 
